include:
  - ../networks.yml
  - ../paradedb-17.yml
  - ../redis-7.yml
x-environment: &environment
  DATABASE_URL: postgresql://${APP_NAME}:${APPS_DATABASE_PASSWORD}@paradedb:5432/${APP_NAME}
  KEY_VAULTS_SECRET: ${APPS_KEY_HEX_16}
  AUTH_SECRET: ${APPS_KEY_HEX_64}
  APP_URL: https://${APP_NAME}.${APPS_DOMAIN}
  INTERNAL_APP_URL: http://localhost:${APP_PORT}
  S3_ENDPOINT: https://${APPS_S3_HOST}
  S3_BUCKET: ${APP_NAME}
  S3_REGION: ${APPS_S3_REGION}
  S3_ACCESS_KEY_ID: ${APPS_S3_ACCESS_KEY_ID}
  S3_SECRET_ACCESS_KEY: ${APPS_S3_SECRET_ACCESS_KEY}
  S3_ENABLE_PATH_STYLE: 1
  S3_SET_ACL: 0
  LLM_VISION_IMAGE_USE_BASE64: 1
  REDIS_URL: redis://redis:6379
  REDIS_PREFIX: lobehub
  REDIS_TLS: 0
  ENABLE_OIDC: 1
  JWKS_KEY: ${LOBEHUB_JWKS_KEY}
  NODE_ENV: production
services:
  lobehub:
    image: lobehub/lobehub
    extends:
      file: ../common.yml
      service: main
    environment: *environment
    depends_on:
      - paradedb
      - redis
