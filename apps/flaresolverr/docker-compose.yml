include:
  - ../networks.yml
x-environment: &environment
  LOG_LEVEL: info
  TZ: ${DAPPS_TIMEZONE}
services:
  flaresolverr:
    extends:
      file: ../common.yml
      service: main
    image: ghcr.io/flaresolverr/flaresolverr
    environment: *environment
