name: CD
on:
  push:
    branches: [ 'main' ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - name: Setup Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_OAUTH_SECRET }}
          tags: tag:ci
          use-cache: 'true'
      - name: Create .env
        uses: SpicyPizza/create-envfile@v2.0
        with:
          envkey_TAILSCALE_OAUTH_CLIENT_ID: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          envkey_DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          file_name: .env
          fail_on_empty: false
          sort_keys: false
      - name: Upload .env via SSH
        uses: appleboy/scp-action@v1
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: root
          key:  ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          source: ".env"
          target: ${{ vars.DEPLOY_PATH }}
      - name: Git pull deploy
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          envs: GITHUB_TOKEN
          script: |
            cd ${{ vars.DEPLOY_PATH }}
            
            git fetch https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git ${{ github.ref_name }}
            git reset --hard FETCH_HEAD
            ./restart.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: rubycats-com/jarvis-report-action@main
        if: success() || failure()
        with:
          report-to: ${{ vars.DAPPS_TELEGRAM_CHAT }}
          token: ${{ secrets.DAPPS_TELEGRAM_TELEGRAM_TOKEN }}
