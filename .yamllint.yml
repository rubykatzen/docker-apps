extends: default
rules:
  empty-lines:
    max: 0
    max-start: 0
    max-end: 1
  trailing-spaces: enable
  line-length: disable
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
  comments:
    min-spaces-from-content: 1
  document-start: disable
