services:
  windows:
    image: dockurr/windows
    restart: unless-stopped
    stop_grace_period: 4m
    environment:
      VERSION: "11"
      CPU_CORES: "4"
      RAM_SIZE: "8G"
      DISK_SIZE: "64G"
      GPU: "Y"
      USERNAME: "admin"
      PASSWORD: ${DAPPS_ADMIN_DEFAULT_PASS}
      LANGUAGE: "Russian"
    devices:
      - /dev/kvm
      - /dev/net/tun
      - /dev/dri
    cap_add:
      - NET_ADMIN
    ports:
      - 8006:8006
      - 3389:3389/tcp
      - 3389:3389/udp
    volumes:
      - ../../apps-data/${APP_NAME}/storage:/storage
