services:
  redis:
    image: redis:7
    restart: unless-stopped
    volumes:
      - ../apps-data/${APP_NAME}/redis:/data
    healthcheck:
      test: [ 'CMD', 'redis-cli', 'ping' ]
      interval: 30s
      timeout: 5s
      retries: 5
    networks:
      - internal
