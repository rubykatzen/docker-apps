include:
  - ../networks.yml
services:
  uptime-kuma:
    extends:
      file: ../common.yml
      service: main
    image: louislam/uptime-kuma:1
    volumes:
      - ../../apps-data/${APP_NAME}/data:/app/data
