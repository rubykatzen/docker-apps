name: Publish
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
concurrency:
  group: publish-docker-apps
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: dupmachine/baseline/.github/actions/publish-app-bundle@latest
        with:
          bundle-name: docker-apps.zip
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }}
          paths: |
            apps
            ansible.cfg
            .env.example
            backup.sh
            deploy.sh
            down.sh
            generate-env.sh
            lib.sh
            logs.sh
            restart.sh
            up.sh
            README.md
