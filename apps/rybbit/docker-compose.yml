include:
  - ../networks.yml
  - ../postgres-17.yml

x-environment: &environment
  NODE_ENV: production
  BASE_URL: https://${APP_NAME}.${DAPPS_DOMAIN}
  BETTER_AUTH_SECRET: ${DAPPS_BETTER_AUTH_SECRET}
  DISABLE_SIGNUP: ${DAPPS_DISABLE_SIGNUP:-false}
  DISABLE_TELEMETRY: ${DAPPS_DISABLE_TELEMETRY:-true}
  MAPBOX_TOKEN: ${DAPPS_MAPBOX_TOKEN}
  POSTGRES_HOST: ${APP_NAME}-postgres
  POSTGRES_PORT: 5432
  POSTGRES_DB: ${APP_NAME}
  POSTGRES_USER: ${APP_NAME}
  POSTGRES_PASSWORD: ${DAPPS_DATABASE_PASSWORD}
  CLICKHOUSE_HOST: http://clickhouse:8123
  CLICKHOUSE_DB: ${APP_NAME}
  CLICKHOUSE_USER: default

services:
  client:
    image: ghcr.io/rybbit-io/rybbit-client
    extends:
      file: ../common.yml
      service: main
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_BACKEND_URL: https://${APP_NAME}.${DAPPS_DOMAIN}
      NEXT_PUBLIC_DISABLE_SIGNUP: ${DAPPS_DISABLE_SIGNUP:-false}
    depends_on:
      - backend

  backend:
    image: ghcr.io/rybbit-io/rybbit-backend
    extends:
      file: ../common.yml
      service: side
    expose:
      - 3001
    environment: *environment
    depends_on:
      clickhouse:
        condition: service_healthy
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3001/api/health"]
      start_period: 30s
      interval: 30s
      timeout: 5s
      retries: 5

  clickhouse:
    image: clickhouse/clickhouse-server:25.4.2
    extends:
      file: ../common.yml
      service: side
    environment:
      CLICKHOUSE_DB: ${APP_NAME}
    volumes:
      - ../../apps-data/${APP_NAME}/clickhouse:/var/lib/clickhouse
    configs:
      - source: clickhouse_network
        target: /etc/clickhouse-server/config.d/network.xml
      - source: clickhouse_json
        target: /etc/clickhouse-server/config.d/enable_json.xml
      - source: clickhouse_logging
        target: /etc/clickhouse-server/config.d/logging_rules.xml
      - source: clickhouse_user_logging
        target: /etc/clickhouse-server/config.d/user_logging.xml
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8123/ping"]
      start_period: 30s
      interval: 30s
      timeout: 5s
      retries: 5
    ulimits:
      nofile:
        soft: 262144
        hard: 262144

configs:
  clickhouse_network:
    content: |
      <clickhouse>
          <listen_host>0.0.0.0</listen_host>
      </clickhouse>
  clickhouse_json:
    content: |
      <clickhouse>
          <settings>
              <enable_json_type>1</enable_json_type>
          </settings>
      </clickhouse>
  clickhouse_logging:
    content: |
      <clickhouse>
        <logger>
            <level>warning</level>
            <console>true</console>
        </logger>
        <query_thread_log remove="remove"/>
        <query_log remove="remove"/>
        <text_log remove="remove"/>
        <trace_log remove="remove"/>
        <metric_log remove="remove"/>
        <asynchronous_metric_log remove="remove"/>
        <session_log remove="remove"/>
        <part_log remove="remove"/>
        <latency_log remove="remove"/>
        <processors_profile_log remove="remove"/>
      </clickhouse>
  clickhouse_user_logging:
    content: |
      <clickhouse>
        <profiles>
          <default>
            <log_queries>0</log_queries>
            <log_query_threads>0</log_query_threads>
            <log_processors_profiles>0</log_processors_profiles>
          </default>
        </profiles>
      </clickhouse>
