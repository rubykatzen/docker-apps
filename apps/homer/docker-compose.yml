include:
  - ../networks.yml
services:
  homer:
    image: b4bz/homer
    extends:
      file: ../common.yml
      service: main
    volumes:
      - ../../apps-data/${APP_NAME}/assets:/www/assets
    labels:
      - traefik.http.routers.${APP_NAME}.middlewares=${APP_NAME}-auth-basic
      - traefik.http.middlewares.${APP_NAME}-auth-basic.basicauth.users=${DAPPS_HTPASSWD}
