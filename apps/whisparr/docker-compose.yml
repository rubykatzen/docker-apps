include:
  - ../networks.yml
services:
  whisparr:
    extends:
      file: ../common.yml
      service: main
    image: ghcr.io/hotio/whisparr:v3
    environment:
      PUID: 1000
      PGID: 1000
      TZ: ${DAPPS_TIMEZONE}
    volumes:
      - ../../apps-data/${APP_NAME}/config:/config
      - ${QBITTORRENT_DOWNLOADS_PATH}:/downloads
      - ${JELLYFIN_ADULT_PATH}:/adult
