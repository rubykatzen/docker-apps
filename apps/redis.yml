services:
  redis:
    image: redis:7
    restart: unless-stopped
    volumes:
      - ../apps-data/${APP_NAME}/redis:/data
    healthcheck:
      test: [ 'CMD', 'redis-cli', 'ping' ]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - internal
